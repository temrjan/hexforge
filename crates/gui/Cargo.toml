[package]
name = "hexforge-gui"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Minimalist GUI for the hexforge vanity Ethereum address forge."

[[bin]]
name = "hexforge"
path = "src/main.rs"

[dependencies]
hexforge-core.workspace = true
# The egui interface and its dependencies are introduced in PR2.
